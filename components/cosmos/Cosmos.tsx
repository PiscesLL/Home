"use client";

/**
 * Cosmos layer.
 *
 * Currently disabled — leaves and stars were removed pending a new
 * visual direction. The shader implementation lives in ShaderBackground
 * and can be re-enabled here when ready.
 */
export function Cosmos() {
  return null;
}
